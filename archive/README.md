# Archive

POOL_MANAGER.md and POOL_WORKER.md: the pre-warmed worker pool experiment (2026-09-16).
Tried, found faster but fragile (silent worker death, wedged supervisor), reverted to the
dispatcher design. Kept for reference.
